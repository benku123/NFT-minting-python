version: '3.8'

services:
  web:
    build: .
    command: gunicorn blockchain.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - DEBUG=1

networks:
  default:
    driver: bridge
